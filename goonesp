-- ==============================================================================
-- Xenoware // Ultimate Unified Suite (Matcha x Unnamed Enhancements)
-- ==============================================================================

local repo = 'https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/'
local Library = loadstring(game:HttpGet(repo .. 'Library.lua'))()
local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera

-- Ablak inicializálása Matcha / UE stílusú sötét / neon dizájnnal
local Window = Library:CreateWindow({
    Title = 'Xenoware // Ultimate Edition [Rivals & Universal]',
    Center = true,
    AutoShow = true,
    TabPadding = 8,
    MenuFadeTime = 0.2
})

local Tabs = {
    Combat = Window:AddTab('Combat'),
    Visuals = Window:AddTab('Visuals'),
    Skins = Window:AddTab('Skins'),
    Character = Window:AddTab('Character'),
    Config = Window:AddTab('Config'),
}

-- ==============================================================================
-- 1. COMBAT TAB (Aimbot, Ragebot, Silent Aim, Triggerbot, Hitbox)
-- ==============================================================================
local CombatLeft = Tabs.Combat:AddLeftGroupbox('Aimbot & Ragebot')
CombatLeft:AddToggle('AimEnabled', { Text = 'Aimbot Enabled (Camera Lock)', Default = false })
CombatLeft:AddToggle('RageEnabled', { Text = 'Ragebot (Above Target Hover + AutoFire)', Default = false })
CombatLeft:AddToggle('TeamCheck', { Text = 'Team Check', Default = true })
CombatLeft:AddToggle('VisibleCheck', { Text = 'Visible Check', Default = true })
CombatLeft:AddSlider('Distance', { Text = 'Max Distance', Default = 800, Min = 50, Max = 2000, Rounding = 0 })
CombatLeft:AddSlider('FOVRadius', { Text = 'FOV Radius', Default = 150, Min = 10, Max = 600, Rounding = 0 })
CombatLeft:AddToggle('ShowFOV', { Text = 'Show FOV Circle', Default = true })
CombatLeft:AddDropdown('HitPart', { Text = 'Target HitPart', Values = {'Head', 'HumanoidRootPart', 'Torso'}, Default = 1 })
CombatLeft:AddSlider('AimSmooth', { Text = 'Aimbot Smoothness', Default = 1, Min = 1, Max = 10, Rounding = 0 })

-- Status kijelző csoport a Combat fülön belül a kért infókkal és színekkel
local StatusGroup = Tabs.Combat:AddLeftGroupbox('Status & Telemetry')
local RageStatusLabel = StatusGroup:AddLabel('Ragebot Status: Disabled')
local ProfileLabel = StatusGroup:AddLabel('Loaded Profile: Xenoware Matcha+UE [Active]')

local CombatRight = Tabs.Combat:AddRightGroupbox('Silent Aim & Triggerbot')
CombatRight:AddToggle('SilentEnabled', { Text = 'Silent Aim Enabled', Default = false })
CombatRight:AddToggle('SilentTeamCheck', { Text = 'Team Check', Default = true })
CombatRight:AddSlider('SilentDist', { Text = 'Silent Distance', Default = 1000, Min = 100, Max = 3000, Rounding = 0 })
CombatRight:AddDropdown('SilentHitPart', { Text = 'Silent HitPart', Values = {'Head', 'HumanoidRootPart'}, Default = 1 })

CombatRight:AddToggle('TriggerEnabled', { Text = 'Triggerbot Enabled', Default = false })
CombatRight:AddToggle('TriggerTeamCheck', { Text = 'Team Check', Default = true })

local HitboxGroup = Tabs.Combat:AddRightGroupbox('Hitbox Expander')
HitboxGroup:AddToggle('HitboxEnabled', { Text = 'Enable Hitbox Expander', Default = false })
HitboxGroup:AddSlider('HitboxSize', { Text = 'Hitbox Scale', Default = 2.0, Min = 1.0, Max = 8.0, Rounding = 1 })
HitboxGroup:AddSlider('HitboxTrans', { Text = 'Hitbox Transparency', Default = 0.6, Min = 0, Max = 1, Rounding = 1 })

-- ==============================================================================
-- 2. VISUALS TAB (ESP & Outlines)
-- ==============================================================================
local VisualsGroup = Tabs.Visuals:AddLeftGroupbox('ESP & Outlines')
VisualsGroup:AddToggle('PlayerOutline', { Text = 'Player Outline ESP', Default = true })

-- ==============================================================================
-- 3. SKINS TAB (Unlock All Skins, Wraps, Charms, Finishers - Client-side)
-- ==============================================================================
local SkinsGroup = Tabs.Skins:AddLeftGroupbox('Client Skin & Cosmetics Unlocker')
SkinsGroup:AddToggle('UnlockAllSkins', { Text = 'Unlock All Weapon Skins', Default = true })
SkinsGroup:AddToggle('UnlockWraps', { Text = 'Unlock All Weapon Wraps', Default = true })
SkinsGroup:AddToggle('UnlockCharms', { Text = 'Unlock All Charms & Keychains', Default = true })
SkinsGroup:AddToggle('UnlockFinishers', { Text = 'Unlock All Kill Finishers & Charms', Default = true })
SkinsGroup:AddButton('Apply All Skins & Cosmetics Now', function()
    -- Teljeskörű kliensoldali inventory / skin override szimuláció
    pcall(function()
        for _, v in pairs(getgc(true)) do
            if typeof(v) == "table" then
                if rawget(v, "Skins") or rawget(v, "WeaponSkins") or rawget(v, "Wraps") then
                    -- Ha a játék inventory tábláját eléri, feloldja lokálisan
                    for key, _ in pairs(v) do
                        v[key] = true
                    end
                end
            end
        end
    end)
    Library:Notify('Minden skin, wrap, charm és finisher sikeresen feloldva (Client-side)!')
end)

-- ==============================================================================
-- 4. CHARACTER TAB (Fly, VoidSpam & Movement)
-- ==============================================================================
local CharGroup = Tabs.Character:AddLeftGroupbox('Movement & Exploits')
CharGroup:AddToggle('FlyEnabled', { Text = 'Advanced Fly', Default = false })
CharGroup:AddSlider('FlySpeed', { Text = 'Flight Speed', Default = 60, Min = 10, Max = 250, Rounding = 0 })
CharGroup:AddToggle('VoidSpam', { Text = 'Void Spam (Teleport Loop)', Default = false })

-- ==============================================================================
-- 5. CONFIG TAB (Theme Manager & Szín Beállítások)
-- ==============================================================================
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ 'ThemeManager_CustomTheme' })
ThemeManager:SetFolder('XenowareMatchaUE')
SaveManager:SetFolder('XenowareMatchaUE/configs')
SaveManager:BuildConfigSection(Tabs.Config)
ThemeManager:ApplyToTab(Tabs.Config)

pcall(function()
    ThemeManager:SetTheme("Default")
end)

-- ==============================================================================
-- LOGIKÁK ÉS HÁTTÉRFOLYAMATOK
-- ==============================================================================

local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Thickness = 1.5
FOVCircle.Color = Color3.fromRGB(0, 255, 128)
FOVCircle.Filled = false
FOVCircle.Transparency = 0.9

local function getBestTarget()
    local bestTarget = nil
    local shortestDist = math.huge
    local mousePos = UserInputService:GetMouseLocation()
    local localChar = LocalPlayer.Character
    
    if not localChar or not localChar:FindFirstChild("HumanoidRootPart") then return nil end
    local localHRP = localChar.HumanoidRootPart

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local humanoid = player.Character:FindFirstChild("Humanoid")
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            
            if humanoid and humanoid.Health > 0 and hrp then
                if Toggles.TeamCheck and Toggles.TeamCheck.Value and player.Team == LocalPlayer.Team and player.Team ~= nil then
                    continue
                end
                
                local dist = (localHRP.Position - hrp.Position).Magnitude
                local maxDist = Options.Distance and Options.Distance.Value or 800
                
                if dist <= maxDist then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                    if onScreen then
                        local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                        local fovLim = Options.FOVRadius and Options.FOVRadius.Value or 150
                        
                        if screenDist <= fovLim and screenDist < shortestDist then
                            shortestDist = screenDist
                            bestTarget = player
                        end
                    end
                end
            end
        end
    end
    return bestTarget
end

-- FOV Kör frissítése
RunService.RenderStepped:Connect(function()
    local mousePos = UserInputService:GetMouseLocation()
    FOVCircle.Position = mousePos
    FOVCircle.Radius = Options.FOVRadius and Options.FOVRadius.Value or 150
    FOVCircle.Visible = Toggles.ShowFOV and Toggles.ShowFOV.Value or false
end)

-- Aimbot, Ragebot és Status kezelés
RunService.RenderStepped:Connect(function()
    local rageActive = Toggles.RageEnabled and Toggles.RageEnabled.Value
    
    -- Status kijelző dinamikus frissítése szöveggel és színjelöléssel
    pcall(function()
        if rageActive then
            RageStatusLabel:SetText("Ragebot Status: Enabled [Hovering & Auto-Firing]")
        else
            RageStatusLabel:SetText("Ragebot Status: Disabled")
        end
    end)

    local target = getBestTarget()
    if target and target.Character then
        local hitPartName = Options.HitPart and Options.HitPart.Value or "Head"
        local part = target.Character:FindFirstChild(hitPartName) or target.Character:FindFirstChild("HumanoidRootPart")
        
        if part then
            local localChar = LocalPlayer.Character
            if localChar and localChar:FindFirstChild("HumanoidRootPart") then
                
                -- Ragebot Logika: A skin / karakter az ellenség fölött helyezkedik el, míg a view/kamera szabadon mozoghat és automatikusan tüzel
                if rageActive then
                    local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
                    if targetHRP then
                        localChar.HumanoidRootPart.CFrame = targetHRP.CFrame + Vector3.new(0, 6, 0)
                        -- Automatikus lövés kiváltása
                        if mouse1click then
                            mouse1click()
                        elseif pressmousebutton1 then
                            pressmousebutton1()
                        end
                    end
                end
                
                -- Szabványos Aimbot (Tiszta kamera aimbot, nem mozgatja a fegyvert/karaktert erőszakosan)
                if Toggles.AimEnabled and Toggles.AimEnabled.Value and not rageActive then
                    local targetPos = part.Position
                    local smooth = Options.AimSmooth and Options.AimSmooth.Value or 1
                    Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, targetPos), 1 / smooth)
                end
            end
        end
    end
end)

-- Silent Aim Hook (Metamethod alapú találat módosítás)
pcall(function()
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        local args = {...}
        local method = getnamecallmethod()
        
        if Toggles.SilentEnabled and Toggles.SilentEnabled.Value then
            if method == "FindPartOnRayWithIgnoreList" or method == "FindPartOnRay" or method == "Raycast" then
                for _, player in ipairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character then
                        if not Toggles.SilentTeamCheck.Value or player.Team ~= LocalPlayer.Team then
                            local hitPartName = Options.SilentHitPart and Options.SilentHitPart.Value or "Head"
                            local part = player.Character:FindFirstChild(hitPartName)
                            if part and method == "Raycast" then
                                args[2] = (part.Position - args[1]).Unit * 1000
                            end
                        end
                    end
                end
            end
        end
        return oldNamecall(self, unpack(args))
    end)
end)

-- Triggerbot Logika
RunService.RenderStepped:Connect(function()
    if Toggles.TriggerEnabled and Toggles.TriggerEnabled.Value then
        local mouse = LocalPlayer:GetMouse()
        local targetPart = mouse.Target
        
        if targetPart and targetPart.Parent then
            local enemyPlayer = Players:GetPlayerFromCharacter(targetPart.Parent)
            if enemyPlayer and enemyPlayer ~= LocalPlayer then
                if not Toggles.TriggerTeamCheck.Value or enemyPlayer.Team ~= LocalPlayer.Team then
                    if mouse1click then
                        mouse1click()
                    elseif pressmousebutton1 then
                        pressmousebutton1()
                    end
                end
            end
        end
    end
end)

-- Hitbox Expander Logika
RunService.RenderStepped:Connect(function()
    if Toggles.HitboxEnabled and Toggles.HitboxEnabled.Value then
        local scale = Options.HitboxSize and Options.HitboxSize.Value or 2.0
        local transp = Options.HitboxTrans and Options.HitboxTrans.Value or 0.6
        
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = p.Character.HumanoidRootPart
                hrp.Size = Vector3.new(2 * scale, 2 * scale, 1 * scale)
                hrp.Transparency = transp
                hrp.CanCollide = false
            end
        end
    end
end)

-- Fly / Repülés Logika
RunService.RenderStepped:Connect(function()
    if Toggles.FlyEnabled and Toggles.FlyEnabled.Value then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
            local hrp = char.HumanoidRootPart
            char.Humanoid.PlatformStand = true
            
            local moveDir = Vector3.new()
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - Camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Camera.CFrame.RightVector end
            
            local speed = Options.FlySpeed and Options.FlySpeed.Value or 60
            hrp.Velocity = moveDir * speed
        end
    else
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid.PlatformStand = false
        end
    end
end)

-- VoidSpam Logika
task.spawn(function()
    while true do
        task.wait(0.05)
        if Toggles.VoidSpam and Toggles.VoidSpam.Value then
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = LocalPlayer.Character.HumanoidRootPart
                hrp.CFrame = hrp.CFrame + Vector3.new(0, -400, 0)
            end
        end
    end
end)

-- Outline ESP Logika
local function applyHighlight(char)
    if not char then return end
    if char:FindFirstChild("XenowareOutline") then return end

    local hl = Instance.new("Highlight")
    hl.Name = "XenowareOutline"
    hl.Adornee = char
    hl.FillTransparency = 1
    hl.OutlineColor = Color3.fromRGB(0, 255, 128)
    hl.OutlineTransparency = 0
    hl.Parent = char
end

task.spawn(function()
    while true do
        task.wait(1)
        if Toggles.PlayerOutline and Toggles.PlayerOutline.Value then
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character then
                    applyHighlight(p.Character)
                end
            end
        else
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character then
                    local hl = p.Character:FindFirstChild("XenowareOutline")
                    if hl then hl:Destroy() end
                end
            end
        end
    end
end)

Library:Notify('Xenoware // Matcha x UE Suite sikeresen betöltődött!')
